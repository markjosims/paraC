# Overview

Modopar is a toolkit for building and applying morphological analyzers using linguistic fieldwork data.
The :doc:`grammar_modules` handle building the parser, and provide user interfaces for (e.g.) defining phoneme inventories, morphological features, paradigms, affixes and morphotactic rules.
The parser can be applied using !TODO